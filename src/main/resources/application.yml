spring:
  application:
    name: test-job-travel

  config:
    import: file:config/cfgDB.yml, file:config/cfgWEB.yml


server:
  error:
    include-message: always
    include-binding-errors: always
